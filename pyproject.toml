[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Section Box"
authors = [{name = "Ruben Messerschmidt", email = "ruben@rubenmesserschmidt.com"}]
dynamic = ["version", "description"]
